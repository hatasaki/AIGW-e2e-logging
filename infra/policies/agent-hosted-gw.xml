<!--
  Foundry Hosted Agent GW policy (placeholders replaced at deploy time).
  1. Validates the BFF's end-user token and derives the trusted Entra oid.
  2. Uses the x-client-* pass-through prefix required by the Hosted Agent gateway.
  3. Rewrites the public Agent GW Responses path to the Foundry Hosted Agent endpoint.
  4. Authenticates APIM to Foundry with APIM's system-assigned managed identity.
-->
<policies>
  <inbound>
    <base />
    <validate-jwt header-name="Authorization" require-scheme="Bearer" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized. A valid Entra ID access token is required." output-token-variable-name="jwt">
      <openid-config url="__OPENID_CONFIG_URL__" />
      <audiences>
        <audience>__AUDIENCE_URI__</audience>
        <audience>__AUDIENCE_APPID__</audience>
      </audiences>
    </validate-jwt>
    <set-header name="x-client-user-oid" exists-action="override">
      <value>@(((Jwt)context.Variables["jwt"]).Claims.GetValueOrDefault("oid", "unknown"))</value>
    </set-header>
    <set-header name="Foundry-Features" exists-action="override">
      <value>HostedAgents=V1Preview</value>
    </set-header>
    <set-query-parameter name="api-version" exists-action="override">
      <value>v1</value>
    </set-query-parameter>
    <rewrite-uri template="/responses" />
    <authentication-managed-identity resource="https://ai.azure.com" ignore-error="false" />
    <llm-emit-token-metric namespace="aigw">
      <dimension name="oid" value='@(((Jwt)context.Variables["jwt"]).Claims.GetValueOrDefault("oid", "unknown"))' />
      <dimension name="API ID" />
      <dimension name="gateway" value="agent" />
    </llm-emit-token-metric>
  </inbound>
  <backend><base /></backend>
  <outbound><base /></outbound>
  <on-error><base /></on-error>
</policies>
