# Focused Microsoft Agent Framework packages used by the agent and official Responses host.
# Avoid the all-integrations meta-package because it adds unrelated adapters to the image.
agent-framework-core==1.11.0
agent-framework-openai==1.10.1
agent-framework-foundry-hosting==1.0.0a260709
httpx==0.28.1
